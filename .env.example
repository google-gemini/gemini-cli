# LM Studio CLI Configuration

# LM Studio Server Configuration
# Default: http://localhost:1234/v1
LM_STUDIO_BASE_URL=http://localhost:1234/v1

# Model name (use "local-model" or the specific model name from LM Studio)
# Default: local-model
LM_STUDIO_MODEL=local-model

# API Key (can be any string for local LM Studio)
# Default: lm-studio
LM_STUDIO_API_KEY=lm-studio

# Optional: Proxy settings if needed
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# Optional: Enable debug logging
# DEBUG=1

# Optional: Disable telemetry (already disabled by default for privacy)
# TELEMETRY_ENABLED=false
