# Run Instructions

1. Open Gemini CLI via `gemini` and perform all the necessary authentication
2. use `/settings` then enable `Show Token Usage` by setting the value to `true`
3. Token Usage and estimate cost will be visible on the right of the Footer
