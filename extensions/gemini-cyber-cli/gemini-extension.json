{
  "name": "gemini-cyber-cli",
  "version": "0.1.0",
  "description": "Ethical hacking extension that bridges Gemini CLI to the HATS framework through MCP.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "hats-mcp-server": {
      "command": "python3",
      "args": ["${extensionPath}${/}mcp${/}hats_mcp_server.py"],
      "cwd": "${extensionPath}"
    }
  }
}
