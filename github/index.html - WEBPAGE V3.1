<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>EcoTecNo Black 360 ® – Andrick IA Máster Control</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Identidad -->
  <meta name="author" content="Alfredo Martínez Pasten (MAPA830324UM9)" />
  <meta name="description" content="Código abierto del agente IA Andrick Máster Control. Entrenamiento IA cerrado bajo Protocolo Regulatory Black 360 ®." />

  <!-- Social -->
  <meta property="og:title" content="EcoTecNo Black 360 ® – Andrick IA Máster Control" />
  <meta property="og:description" content="Código abierto. Entrenamiento IA cerrado. Interacción ética y responsable con IA." />
  <meta property="og:type" content="website" />

  <!-- Manifest -->
  <link rel="manifest" href="/manifest.json" />

  <!-- Estilos -->
  <style>
    body {
      margin: 0;
      background: #050608;
      color: #f5f5f5;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    header {
      padding: 2rem;
      border-bottom: 1px solid #222;
    }
    .brand {
      font-size: 1.4rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    .tagline {
      margin-top: 0.5rem;
      color: #999;
    }
    main {
      max-width: 960px;
      margin: 2rem auto;
      padding: 0 1.5rem;
    }
    h1 {
      font-size: 1.8rem;
      margin-bottom: 0.5rem;
    }
    .badge {
      display: inline-block;
      padding: 0.25rem 0.6rem;
      border-radius: 999px;
      background: #111;
      border: 1px solid #444;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #ccc;
    }
    .disclosure {
      margin-top: 1rem;
      padding:
