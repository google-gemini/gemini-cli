# Accelos AI Agent Environment Variables

# LLM API Keys
GOOGLE_API_KEY=your_google_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

#   When you run npm install -g @anthropic-ai/claude-code, here's what happens:
#   Current System (macOS with Homebrew npm):
#   - Global npm prefix: /opt/homebrew
#   - Claude executable symlink: /opt/homebrew/bin/claude
#   - Actual executable: /opt/homebrew/lib/node_modules/@anthropic-ai/claude-code/cli.js
#   - The symlink points to: ../lib/node_modules/@anthropic-ai/claude-code/cli.js
#
# Claude Code Configuration
# Path to Claude Code executable (optional)
# If not set, Claude Code will use its default discovery mechanism
# 
# Option 1: Use command name (works if Claude Code is in PATH after global install)
# CLAUDE_CODE_EXECUTABLE_PATH=claude
#
# Option 2: Use absolute path (find your path with: which claude)
# Common paths after npm install -g @anthropic-ai/claude-code:
# - macOS (Homebrew):     /opt/homebrew/bin/claude
# - macOS (Node.js):      /usr/local/bin/claude  
# - Linux:                /usr/local/bin/claude
# - Windows:              %APPDATA%\npm\claude.cmd
CLAUDE_CODE_EXECUTABLE_PATH=/opt/homebrew/bin/claude

# Data Directory Configuration
# Main data directory path (defaults to ./.accelos/data if not set)
ACCELOS_DATA_DIRECTORY_PATH=./.accelos/data

# Legacy/Deprecated Configuration (use ACCELOS_DATA_DIRECTORY_PATH instead)
# ACCELOS_GUARDRAIL_FILE_PATH=./src/prompts/guardrails.json
# RCA_DIRECTORY_PATH=./src/rcas

# GitHub MCP Configuration
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_MCP_SERVER_URL=http://localhost:3001/mcp
# Alternative: For subprocess mode
# GITHUB_MCP_COMMAND=npx github-mcp-server

# Mastra Server Configuration
MASTRA_PORT=4111
MASTRA_HOST=0.0.0.0

# Neo4j Database Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=pass-here
NEO4J_DATABASE=neo4j