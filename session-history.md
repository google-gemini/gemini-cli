### 2026-08-12 17:17 — main (c09c9b233 "fix(anthropic): map deduplicated tool_use IDs to matching functionResponse tool_result blocks")
- [x] Fixed startup health check hang by reducing timeouts in
- [x] Fixed 401 Unauthorized API error on Vertex AI when `GOOGLE_API_KEY`
- [x] Verified MCP client manager tool discovery and session reloading with unit
- [x] Verified build pipeline (`npm run bundle`) completes cleanly.
---

### 2026-08-12 16:37 — main (969f59589 "fix(anthropic): deduplicate tool_use IDs and improve PTY teardown reliability")
- [x] Fixed startup health check hang by reducing timeouts in
- [x] Fixed 401 Unauthorized API error on Vertex AI when `GOOGLE_API_KEY`
- [x] Verified MCP client manager tool discovery and session reloading with unit
- [x] Verified build pipeline (`npm run bundle`) completes cleanly.
---

### 2026-08-12 16:36 — main (969f59589 "fix(anthropic): deduplicate tool_use IDs and improve PTY teardown reliability")
- [x] Fixed startup health check hang by reducing timeouts in
- [x] Fixed 401 Unauthorized API error on Vertex AI when `GOOGLE_API_KEY`
- [x] Verified MCP client manager tool discovery and session reloading with unit
- [x] Verified build pipeline (`npm run bundle`) completes cleanly.
---

